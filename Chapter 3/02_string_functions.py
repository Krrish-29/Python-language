str ='k'
print(len(str)) #– This function returns the length of the strings.

print(str.endswith("rry"))
#This function_ tells whether the variable string ends with the string "r" or not.(boolean)

print(str.count("r")) # counts the total number of occurrences of any character

print(str.capitalize()) # the first character of a given string is capitalized

print(str.find("rr"))
#This function find a word and returns the index of first ocurrence of that word in the string.

print(str.replace("r", "l"))
#This function replace the old word with new word in the entire string.(old word, new word )